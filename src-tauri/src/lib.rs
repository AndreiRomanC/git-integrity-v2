mod local_drive;
mod repository;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .invoke_handler(tauri::generate_handler![
            repository::build_info,
            repository::frontend_perf_log,
            repository::choose_folder,
            repository::init_repository,
            repository::clone_repository,
            repository::open_external_url,
            repository::open_repository_item,
            repository::open_submodule_on_server,
            repository::open_commit_on_server,
            repository::open_repository_fast,
            repository::load_repository,
            repository::load_older_commits,
            repository::tag_details,
            repository::refresh_status,
            repository::stage_files,
            repository::stage_all,
            repository::unstage_files,
            repository::create_commit,
            repository::branches::branch_creation_context,
            repository::branches::graph_branch_divergence,
            repository::branches::create_branch,
            repository::branches::switch_branch,
            repository::load_directory,
            repository::list_directory_fast,
            repository::submodule_navigation_status,
            repository::submodule_folder_status,
            repository::entry_details,
            repository::entry_last_commit,
            repository::submodule_versions,
            repository::add_submodule,
            repository::switch_submodule_version,
            repository::reset_submodule,
            repository::reset_submodule_branch_to_upstream,
            repository::change_submodule_url,
            repository::remove_git_path,
            repository::delete_local_path,
            repository::commit_path,
            repository::commit_files,
            repository::commit_staged,
            repository::restore_file,
            repository::restore_remote_file,
            repository::path_history,
            repository::compare_remote_directory,
            repository::compare_file_contents,
            repository::read_text_file,
            repository::write_text_file,
            repository::list_remotes,
            repository::pr_status,
            repository::fetch_remote,
            repository::fetch_all_remotes,
            repository::sync_repository,
            repository::publish_status,
            repository::publish_branch,
            repository::submodule_publish_risks,
            repository::submodule_repository,
            repository::stash::stash_changes,
            repository::stash::stash_file,
            repository::stash::list_stashes,
            repository::stash::list_submodule_stashes,
            repository::stash::pop_stash,
            repository::stash::drop_stash,
            repository::stash::restore_stash_paths,
            repository::stash::abort_stash_conflict,
            repository::stash::stash_entry_files,
            repository::branches::rename_branch,
            repository::branches::delete_branch,
            repository::commit_submodule,
            repository::push_submodule,
            repository::push_submodule_preview,
            repository::fetch_submodule,
            repository::pull_submodule,
            repository::force_push_submodule,
            repository::file_blame,
            repository::merge_branch,
            repository::merge_in_progress,
            repository::list_conflicts,
            repository::conflict_sides,
            repository::resolve_conflict,
            repository::complete_merge,
            repository::abort_merge,
            repository::branches::create_submodule_branch,
            repository::create_submodule_tag,
            repository::push_submodule_tag,
            repository::run_git_command,
            local_drive::list_local_directory,
            local_drive::create_local_directory,
            local_drive::copy_local_item,
            local_drive::move_local_item,
            local_drive::trash_local_item,
            local_drive::read_local_text_file,
            local_drive::write_local_text_file,
            repository::run_terminal_command,
            repository::run_utrud,
        ])
        .run(tauri::generate_context!())
        .expect("error while running Git DrillDown");
}
